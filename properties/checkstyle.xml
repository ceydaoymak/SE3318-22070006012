<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Puppy Crawl//DTD Check Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">
    <module name="TreeWalker">

        <!-- This rule checks that variables are used near where they are declared -->
        <module name="VariableDeclarationUsageDistance" />

        <!-- This rule gives error if there are empty blocks like: if () {} -->
        <module name="EmptyBlock">
            <property name="option" value="text"/>
            <property name="tokens" value="
                LITERAL_TRY,
                LITERAL_CATCH,
                LITERAL_FINALLY,
                LITERAL_IF,
                LITERAL_ELSE,
                LITERAL_WHILE,
                LITERAL_FOR,
                LITERAL_DO,
                STATIC_INIT,
                INSTANCE_INIT,
                LITERAL_SWITCH
            "/>
        </module>

        <!-- This rule checks if you change the loop variable inside the loop (which is wrong) -->
        <module name="ModifiedControlVariable" />

    </module>
</module>
